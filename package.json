{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "purescript-psa": "^0.7.3",
    "purescript": "^0.13.0",
    "pulp": "^13.0.0"
  }
}
